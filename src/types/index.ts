/**
 * For Types shared across different components. Type definitions needed in a single file location are left
 * in their respective files.
 */

// defines info needed to create and share a socket
export type SocketType = {
  name: string,
  path: string
}